{
  "name": "github-search",
  "version": "1.0.0",
  "description": "web app with react for searching github user",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "young-utf",
  "license": "ISC",
  "dependencies": {
    "react": "^0.13.3"
  }
}
